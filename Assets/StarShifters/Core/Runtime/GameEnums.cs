// Используем неймспейс проекта, чтобы избежать конфликтов имен
namespace StarShifters.Core.Runtime
{
	/// <summary>
	/// Тип карты: Движение / Защита / Баф.
	/// </summary>
	public enum CardType
	{
		Movement = 0,   // Уменьшает "Оставшуюся дальность полёта" (ХП маршрута) — см. правила. 
		Defense = 1,    // Щит/защита от "отбрасывания" и прочих негативных эффектов.
		Buff = 2        // Временные/постоянные бафы: энергия на ход(ы), бонусы к движению/защите и т.п.
	}

	/// <summary>
	/// Условный тип угрозы (на будущее). Можно расширять по мере добавления контента.
	/// </summary>
	public enum ThreatType
	{
		Generic = 0,
		KnockBack = 1,   // "Отбрасывание назад" — добавляет очки маршрута, если нет щитов.
		EnergyPenalty = 2, // Штраф к энергии текущего/след. ходов.
		Custom = 100
	}
}
